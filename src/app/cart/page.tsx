import { PlaceholderPage } from "@/components/common/PlaceholderPage";

export const metadata = {
  title: "Cart — SURFER",
  description: "Review your tailored selections.",
};

export default function CartPage() {
  return (
    <PlaceholderPage
      title="Bespoke Bag"
      subtitle="Your curated garment selections will appear here once the ordering window opens."
      category="BAG"
    />
  );
}
